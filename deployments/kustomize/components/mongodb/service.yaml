kind: Service
apiVersion: v1
metadata:
  name: &PODNAME mongodb
  labels:
    pod: mongodb
spec:
  selector:
    pod: *PODNAME
  ports:
  - name: mongo
    protocol: TCP
    port: 27017
    targetPort: mongodb-port
    