kind: Service
apiVersion: v1
metadata:
  name: ak-ambulance-webapi
spec:
  selector:
    pod: ak-ambulance-webapi-label
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: webapi-port