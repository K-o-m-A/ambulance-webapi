apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- deployment.yaml
- service.yaml

configMapGenerator:
  - name: ak-ambulance-webapi-mongodb-init
    files:
      - params/init-db.js
  - name: ak-ambulance-webapi-config
    literals:
      - database=ak-ambulance
      - collection=ambulance
patches:
- path: patches/webapi.deployment.yaml
  target:
    group: apps
    version: v1
    kind: Deployment
    name: ak-ambulance-webapi